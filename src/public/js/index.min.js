const showMenu = (toggleId, navId) =>{
   const toggle = document.getElementById(toggleId),
      nav = document.getElementById(navId);
   
   if(toggle && nav){
      toggle.addEventListener('click', () =>{
         nav.classList.toggle('show-menu');
      });
   }
};

showMenu('nav-toggle','nav-menu');

/*==================== SWIPER JS ====================*/
let galleryThumbs = new Swiper('.gallery-thumbs', {
   spaceBetween: 0,
   slidesPerView: 0,
});

let galleryTop = new Swiper('.gallery-top', {
   effect: 'fade',
   loop: true,

   thumbs: {
      swiper: galleryThumbs
   }
});

/*==================== GSAP ANIMATION ====================*/
const controlImg = document.querySelectorAll('.controls__img');

function scrollAnimation(){
   // gsap.from('.events__subtitle', {opacity: 0, duration: .2, delay: .2, y: -20});
   gsap.from('.events__title', {opacity: 0, duration: .3, delay: .3, y: -20});
   gsap.from('.events__description', {opacity: 0, duration: .4, delay: .4, y: -20});
   // gsap.from('.events__button', {opacity: 0, duration: .5, delay: .5, y: -20});
   // gsap.from('.events__video-content', {opacity: 0, duration: .6, delay: .6, y: -20});

   // eventsPopup.classList.remove('show-popup');
}

controlImg.forEach(c => c.addEventListener('click', scrollAnimation));
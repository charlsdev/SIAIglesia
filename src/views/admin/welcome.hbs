      <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" type="text/css">
      <link rel="stylesheet" href="/sweetalert2/sweetalert2.min.css" type="text/css">
      <link rel="stylesheet" href="/boxicons/boxicons.min.css" type="text/css">
      <link rel="stylesheet" href="/css/navSecretaria.min.css" type="text/css">
      <link rel="stylesheet" href="/css/counterAnimation.min.css" type="text/css">
      <link rel="stylesheet" href="/css/clock.min.css" type="text/css">
      
   </head>

	<body>
      {{> loader}}
      {{> navBarA}}

      <div class="container p-30">
         <div class="row">
            <div class="col-md-6 mx-auto">
               {{> alerts}}
               {{> errors}}
            </div>
         </div>
         
         {{> clock}}

         <div class="animation">
            <div class="counter">
               <div class="counter-number">{{admin}}</div>
               <h3>Administradores</h3>
            </div>

            <div class="counter">
               <div class="counter-number">{{sace}}</div>
               <h3>Sacerdotes</h3>
            </div>

            <div class="counter">
               <div class="counter-number">{{secre}}</div>
               <h3>Secretarias</h3>
            </div>
         </div>

         {{!-- <div class="row mt-3">
            <div class="col-md-12 d-flex justify-content-center">
               <img src="/img/SMMIglesia.png" alt="Imagen de la iglesia" class="img__iglesia">
            </div>
         </div> --}}
      </div>

      {{!-- <br><br> --}}

      <script src="/js/jquery-3.5.1.min.js"></script>
      <script src="/js/popper.min.js"></script>
      <script src="/bootstrap/js/bootstrap.min.js"></script>
      <script src="/sweetalert2/sweetalert2.min.js"></script>
      <script src="/fontawesome/js/all.min.js"></script>
      <script src="/js/navSecretaria.min.js"></script>

      <script type="text/javascript">
         $(document).ready(function() {
            $('.counter-number').each(function() {
               $(this).prop('counter', 0).animate({
                  counter: $(this).text()
               }, {
                  duration: 1000,
                  easing: 'swing',
                  step: function(now) {
                     $(this).text(Math.ceil(now) + '');
                  }
               });
            });
         });
      
         let hour = parseInt({{h}}),
            min = parseInt({{m}}),
            sec = parseInt({{s}});
      
         setInterval(() => {
            if (sec < 59) {
               sec = sec + 1;
            } else {
               sec = 0;
               if (min < 59) {
                  min = min + 1;
               } else {
                  min = 0;
                  if (h < 23) {
                     h = h + 1;
                  } else {
                     h = 0;
                  }
               }
            }

            document.querySelector(".hour_num").innerText = (hour > 9) ? hour : `0${hour}`;
            document.querySelector(".min_num").innerText = (min > 9) ? min : `0${min}`;
            document.querySelector(".sec_num").innerText = (sec > 9) ? sec : `0${sec}`;
         }, 1000);
      </script>